# What are the required components of an HTTP request?
# What are the additional optional components?
 
# Required: request method used, path, HTTP version used (for HTTP 1.0 and later),
#           host header (for HTTP 1.1 and later)

# Optional: additional headers, query parameters for path, message body (for POST requests)
